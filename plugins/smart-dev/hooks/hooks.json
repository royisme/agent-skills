{
  "description": "Feature Development Workflow hooks for progress tracking, session recovery, and loop control",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "description": "Check for incomplete feature development on session start",
            "command": "bash ~/.claude/plugins/cache/roy-agent-skills/smart-dev/2.0.0/scripts/check-progress.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "description": "Prevent exit during active loop iterations",
            "command": "bash ~/.claude/plugins/cache/roy-agent-skills/smart-dev/2.0.0/scripts/loop-stop.sh"
          },
          {
            "type": "prompt",
            "description": "Update progress tracking for active features",
            "prompt": "Check if there are any in-progress features in .works/spec/*/progress.md. If found, update the progress.md file with current state, phase, and any important context that would help resume the work later."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "description": "Block spec edits after readiness gate passes",
            "command": "bash ~/.claude/plugins/cache/roy-agent-skills/smart-dev/2.0.0/scripts/guard-writes.sh"
          }
        ]
      }
    ]
  }
}
